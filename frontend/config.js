// API Configuration
// This file is used to configure the API URL for the frontend
// For local development, this will use localhost
// For production (Netlify), this will be replaced with the actual API URL

// Set API URL from environment variable or use default
window.API_URL = window.API_URL || 'http://localhost:8080';

